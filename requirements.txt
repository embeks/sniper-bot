# requirements.txt - Phase 1 Solana Sniper Bot
# Core Solana libraries
solana==0.32.0
solders==0.20.0

# Web and async
websockets==10.4
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
base58==2.1.1

# Note: Removed unused dependencies from old version
# - httpx (not needed for v2)
# - certifi (handled by aiohttp)
# - fastapi/uvicorn (no API in Phase 1)
# - psutil (not monitoring system resources)
